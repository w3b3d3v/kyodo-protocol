import { useVaultContract } from "../ContractContext"

function Balances(props) {
  // const { contract, loading } = useVaultContract();
  // console.log("Balance of user: ",  contract)

  return (
    <div>
      <h1>Balances</h1>
      <p>List of Balances</p>
    </div>
  );
}

export default Balances;